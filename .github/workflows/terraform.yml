# This workflow installs the latest version of Terraform CLI and configures the Terraform CLI configuration file
# with an API token for Terraform Cloud (app.terraform.io). On pull request events, this workflow will run
# `terraform init`, `terraform fmt`, and `terraform plan` (speculative plan via Terraform Cloud). On push events
# to the "master" branch, `terraform apply` will be executed.
#
# Documentation for `hashicorp/setup-terraform` is located here: https://github.com/hashicorp/setup-terraform
#
# To use this workflow, you will need to complete the following setup steps.
#
# 1. Create a `main.tf` file in the root of this repository with the `remote` backend and one or more resources defined.
#   Example `main.tf`:
#     # The configuration for the `remote` backend.
#     terraform {
#       backend "remote" {
#         # The name of your Terraform Cloud organization.
#         organization = "example-organization"
#
#         # The name of the Terraform Cloud workspace to store Terraform state files in.
#         workspaces {
#           name = "example-workspace"
#         }
#       }
#     }
#
#     # An example resource that does nothing.
#     resource "null_resource" "example" {
#       triggers = {
#         value = "A example resource that does nothing!"
#       }
#     }
#
#
# 2. Generate a Terraform Cloud user API token and store it as a GitHub secret (e.g. TF_API_TOKEN) on this repository.
#   Documentation:
#     - https://www.terraform.io/docs/cloud/users-teams-organizations/api-tokens.html
#     - https://help.github.com/en/actions/configuring-and-managing-workflows/creating-and-storing-encrypted-secrets
#
# 3. Reference the GitHub secret in step using the `hashicorp/setup-terraform` GitHub Action.
#   Example:
#     - name: Setup Terraform
#       uses: hashicorp/setup-terraform@v1
#       with:
#         cli_config_credentials_token: ${{ secrets.TF_API_TOKEN }}

name: Terraform

on:
  push:
    branches:
      - master
  pull_request:

permissions:
  contents: read

jobs:
  terraform-plan:
    name: Plan - ${{ matrix.working-directory }}
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        working-directory:
          - 01-alz
          - 02-connectivity
          - 03-application

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verify Terraform configuration
        shell: bash
        run: |
          directory="${{ matrix.working-directory }}"

          if [ ! -d "$directory" ]; then
            echo "::error::Directory '$directory' does not exist."
            exit 1
          fi

          if ! find "$directory" -maxdepth 1 -name '*.tf' -print -quit | grep -q .; then
            echo "::error::No Terraform files found in '$directory'."
            exit 1
          fi

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          cli_config_credentials_token: ${{ secrets.TF_API_TOKEN }}

      - name: Terraform Format
        run: terraform -chdir=${{ matrix.working-directory }} fmt -check

      - name: Terraform Init
        run: terraform -chdir=${{ matrix.working-directory }} init -input=false

      - name: Terraform Validate
        run: terraform -chdir=${{ matrix.working-directory }} validate

      - name: Terraform Plan
        run: terraform -chdir=${{ matrix.working-directory }} plan -input=false

  terraform-apply:
    name: Apply
    runs-on: ubuntu-latest
    needs: terraform-plan

    if: github.ref == 'refs/heads/master' && github.event_name == 'push'

    environment: production

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          cli_config_credentials_token: ${{ secrets.TF_API_TOKEN }}

      # Apply sequentially so foundational infrastructure is completed first.
      - name: Terraform Apply
        shell: bash
        run: |
          set -euo pipefail

          directories=(
            "01-alz"
            "02-connectivity"
            "03-application"
          )

          for directory in "${directories[@]}"; do
            echo "::group::Applying $directory"

            terraform -chdir="$directory" init -input=false
            terraform -chdir="$directory" apply \
              -auto-approve \
              -input=false

            echo "::endgroup::"
          done
